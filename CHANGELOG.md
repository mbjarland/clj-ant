# Change Log

clj-ant has not yet cut a versioned release. Until it does, follow
`git log --oneline` for the canonical change history.

When a versioned release is cut this file will follow the
[keepachangelog.com](http://keepachangelog.com/) conventions.
